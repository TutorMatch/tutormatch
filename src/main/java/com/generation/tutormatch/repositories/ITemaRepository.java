package com.generation.tutormatch.repositories;

import com.generation.tutormatch.services.TemaDao;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ITemaRepository extends JpaRepository<TemaDao, Integer> {
}
